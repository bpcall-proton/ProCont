name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install
        run: npm ci

      - name: Package Windows
        run: npm run desktop:win

      - name: Publish Windows
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          Get-ChildItem "release/Fatture-Incassi-Pro-Setup-*.exe" |
            ForEach-Object {
              gh release upload "$env:GITHUB_REF_NAME" $_.FullName --clobber
            }

  macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install
        run: npm ci

      - name: Package macOS
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npm run build && npx electron-builder --mac dmg --x64 --arm64

      - name: Publish macOS
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "$GITHUB_REF_NAME" release/Fatture-Incassi-Pro-*.dmg --clobber

  linux:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install
        run: npm ci

      - name: Package Linux
        run: npm run build && npx electron-builder --linux AppImage --x64

      - name: Publish Linux
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "$GITHUB_REF_NAME" release/Fatture-Incassi-Pro-*.AppImage --clobber

  android:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Java
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407

      - name: Android platform
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Install
        run: npm ci

      - name: Package Android
        run: npm run android:apk

      - name: Prepare Android
        run: |
          version="${GITHUB_REF_NAME#v}"
          mkdir -p release/mobile
          cp android/app/build/outputs/apk/debug/app-debug.apk \
            "release/mobile/Fatture-Incassi-Pro-${version}-Android.apk"

      - name: Publish Android
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "$GITHUB_REF_NAME" release/mobile/Fatture-Incassi-Pro-*-Android.apk --clobber
