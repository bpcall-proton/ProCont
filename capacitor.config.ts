import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.bpcall.fattureincassipro',
  appName: 'Fatture & Incassi Pro',
  webDir: 'dist',
  android: {
    allowMixedContent: false,
  },
}

export default config
