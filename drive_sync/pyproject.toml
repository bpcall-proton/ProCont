[project]
name = "fatture-incassi-drive-sync"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "cryptography==45.0.6",
  "fastapi==0.116.1",
  "httpx==0.28.1",
  "uvicorn[standard]==0.35.0",
]

[tool.uvicorn]
factory = false
